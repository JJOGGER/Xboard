xboard是一套完善的系统，但是前端代码是封装的，我重新开发了一套新的管理系统和前端网站，但是后台代码保持xboard不做调整（称为新系统）；
然后在新系统增加了共享订阅的功能，跟之前传统套餐不同，他是导入订阅后，用户通过下单完成便可之间导入使用，不需要自己配置节点；
共享订阅下单和支付等可以使用旧系统已有功能支持，但是需要用Share重新创建对应的套餐、支付接口service等，如OrderService需要再额外创建ShareOrderService，和旧系统只做差异化处理，流程基本一致，接口使用新的接口